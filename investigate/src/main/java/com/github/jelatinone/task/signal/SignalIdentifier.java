package com.github.jelatinone.task.signal;

public enum SignalIdentifier {
	URL_PATH_DEPTH,
	URL_PATH_NAME,
	DOMAIN_NAME,
	REDIRECT_HOPS,
	SET_COOKIE,
	CONTENT_TYPE,
	CONTENT_LENGTH,
	ANCHOR_DENSITY
}
